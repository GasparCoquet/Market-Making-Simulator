# Runtime dependencies, kept in sync with [project.dependencies] in
# pyproject.toml. `pip install -e .` is the supported install path; this file
# exists for readers who expect it.
numpy>=1.21,<3.0
pandas>=1.3,<4.0
matplotlib>=3.4,<4.0
